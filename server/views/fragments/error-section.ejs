<% if (errors.length != 0) { %>
    <div class="errors">
      Please fix the following errors:
      <ul>
        <% errors.forEach(error => { %>
         <%- `<li> ${error} </li>` %> 
        <% }) %>
      </ul>
    </div>
  <% } %>